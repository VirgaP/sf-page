<form method="post" action="{{ path('user_message_delete', {'id': userMessage.id}) }}" onsubmit="return confirm('Ar tikrai norite ištrinti šią žinutę?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ userMessage.id) }}">
    <button class="btn btn-danger btn-sm">Ištrinti</button>
</form>